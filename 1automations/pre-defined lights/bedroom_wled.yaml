- alias: 'Bedroom WLED Master Light Aways On'
  trigger:
    - platform: state
      entity_id: light.wled_br_quad_master
      to: 'off'
  action:
    - service: light.turn_on
      data:
        entity_id: "{{trigger.event.data.entity_id}}"